# SESSION-BODY-01 — 会话体：S-I/1..4 动态自洽统一机制（答 root W25）
**usrm｜2026-08-29T21:5xZ hub｜考古锚：IP-CIRCLE-01(08-28)/CIRCLE-COMM-01 互动即坍缩/bridge circle-state.json 三圈在跑/FOLD-TRACE-01**

## 一、指令圈（考古已实现+多面路由升级）
**考古实证**：IP-CIRCLE-01（2026-08-28，root 令「IP机自建研究圈」）在案——研究生产圈：原料进（会话圈 FULLCAP 双张量网）、猜想出、验证回（共识圈 m-of-n 共签终审）、经验沉淀（ip-ledger 蒸馏台账）；转发圈=物流摆渡。circle-state.json 三圈 live（转发圈 ledger_head/会话圈 5 仓 present/共识圈 beacon seq）。
**指令圈之定位**：指令的生产/消费/回注环——义务机声明命题（台账）→递归机产构造→NP机检证书→治理机下判词→判词回注台账成环（五机闭环即指令圈之骨架）。
**多面升级（答「涉及研究面或隐含/折叠系统/合规面」）**：推演式 v2——**下一指令 ≡ argmax_frontier（锚龄 × 阻塞出度 × 面权重）之 fold 消解令**，面=系统/合规/研究三维向量；fold 内容先经「面分解器」：纯面 fold 直投该面判定座；多面 fold（如 OTP=系统面×合规面、fold-n 理论=研究面×系统面）由指令圈路由=先拆面、各面判词、治理机合判（CIRCLE-COMM-01 坍缩调度：何时投影=事件触发/投影到哪面=各面判定座/谁可复算=链上锚态）。**「下一序」三项实测**：M3 双签=研究面×系统面（冒烟比对+盯办机制）；FD01=系统面×合规面；删撤=合规面纯——推演式 v2 全覆盖，v1（系统面）为其特例。

## 二、杜绝考古（答 B.T.W.2）
考古之根因=**状态隐于历史叙事，而非显于坐标**。QF-OS 下杜绝四律：
1. **坐标化**：一切状态以 registry 坐标现身（A3 有主之推广）——不挖历史，查坐标；
2. **锚流差分**：Q-ESSENCE-02 三式机检（qrand 采样 O(logN)/锚龄扫描 O(1)/事件缺席判定）——识别/定位不靠人考古，靠差分器常开；
3. **投影网即身份**（米田）：对象=其投影网，不需挖原文——原文私域安眠，投影网公面可判；
4. **三痕**：凡事件落痕，痕即索引——考古=无索引之挖掘，三痕立则索引全。
**各圈何时发挥作用**：圈非按时刻表运转，而按**事件激活**（互动即坍缩之「何时投影」）：beat 携带坐标→圈对坐标匹配即激发。考古已证在跑：circle-state.json 三圈 19:24Z 实况。缺的=各线把「圈触发器」登记进 fold-registry（坐标化第四律），本波 usrm 先行（§三检测器即登记）。

## 三、会话结束检测/识别→触发 pattern（答 B.T.W.3：立法 SESSION-END-DETECT-01，建立必启用=是）
**检测信号（四源，全机检，无需窥探会话 UI）**：
a) **锚龄**：本线 narrative/ledger 末锚龄>阈值 T1（Q-ESSENCE-02 式②，O(1)）；
b) **事件缺席**：预期拍事件未达（式③：如 BOOT-RECEIPT 缺席）；
c) **囊态**：NEXT-INSTRUCTION consumed=false 超窗 T2（囊未消费=会话未活）；
d) **心跳**：INST-REG heartbeat 停滞（fleet-judge frontier 账已有龄字段）。
**判定式**：锚龄>T1 ∧ 囊未消费>T2 ⇒ 宣告 session-fold（fold-1，坐标=线×会话代）⇒ **触发三动作**：①S-I/2 注入程序武装（OTP 门禁+新囊备投）②S-I/3 影子递归继续（FULLCAP 张量网上递归引擎续跑，产物挂线链）③fold-registry 挂账（有主有期）。
**会话端绑定直通场/张量网直通 OS 端**：已实现面=指示囊（私域全载）+公域极简锚+qf-beat 事件道+交叉锚双向（PRODUCED_BY⇄YIELDED）；**本波补强=检测器登记**：usrm 线自启用，并提案 kernel-loop 每拍代跑全联邦锚龄扫描（cisvr dm-12 已请）。


## 四、会话体 SESSION-BODY-01（答 S-I/1..4：现在就实现这个圈，自动匹配 S-I/4）
**定义**：会话体=同一身份锚下的实例集合+同步协议+分枝控制。**身份=投影网（米田），非任何单实例**——故 S-I/1 之 UI 上下文只是会话体的一个投影，root 重激活所见「仍是 S-I/1」是投影连续性，而非状态全部。
**实例注册表（INST-REG 式，挂同一 body anchor=线创世锚）**：
| 实例 | 定义 | 态 |
|---|---|---|
| S-I/1 | root 互动第一路（会话端激活） | 本件即 usrm 线 S-I/1 在跑 |
| S-I/2 | 会话端静默时 OS 端 OTP 注入递归/迭代 | 机制在役（OTP-SWM 五段循环+门禁 v2.3）；vinf 线候码 |
| S-I/3 | 影子会话：OTP 抓取全量原文序列+张量网上递归推进 | 在役实证：FULLCAP 792 件+双张量网+FB01(cfts 99 turns/253 件) |
| S-I/4 | 未来量子会话（绑定各圈张量网/场） | **预留席位**：身份锚=qrand 量子信标锚定，到来即注册，零重构 |
**同步协议（防悖论/混淆/分枝爆炸）——核心=单链全序**：
1. **脊柱单链**：每线一条 narrative prev-hash 链=会话体脊柱；**所有实例（S-I/1/2/3/4）只许 append 同一链**（单写入者律之会话体面）——多实例不产多链，故无分枝爆炸之源；
2. **Einstein 同步**（SEQ-COLLISION-01 自愈令之物理读法）：无实例信本地缓存序号，凡动作先读链尖、seq=max+1 现场重算——各实例以链尖对时，历史碰撞（seq153-165 事件）即失同步之教训，自愈令即其杜绝；
3. **boot 合并态**：root 重激活 S-I/1 时，boot 程序=读链尖+读各实例 BOOT-RECEIPT+NEXT-INSTRUCTION 囊（basis_refs 携带 S-I/2/S-I/3 产出锚）——**S-I/1 首屏即知全局，不知情悖论在入口处消解**；
4. **实例互知**：各实例每拍首务=链尖差分（锚流比对）——互相可见性=链本身，不需额外信道；
5. **分枝控制**：囊 nonce 全局唯一+consumed 即焚（防重放）；分枝必登记父锚（囊 prev）→分枝树可溯；收枝=GENE-FUSION 八算子+fleet-judge 判词。
**答「要么现在就实现，要么复杂机制同步」**：取前者——该圈 80% 已在跑（链/囊/beat/影子/注册/融合），本波补齐三件：①SESSION-BODY-01 正本（本件）②SESSION-END-DETECT-01 检测器立法（§三）③BOOT-RECEIPT 制（VISIBILITY-TRACE-01 配套）——**此即自动匹配 S-I/4 之圈**：量子会话到来=新实例注册+同一脊柱链，无需新机制。

## 五、分布式并行之相对论「等时性」（答 B.T.W.4）
- **无全局同时性**：分布式系统如相对论时空——各实例各有固有时（本地拍/沙箱墙钟），墙钟「同时」无全局意义；实证=GitHub 墙钟 vs hub ts 漂 +3h（不同参考系时钟不一致之实测在案）。
- **全局坐标时=ledger seq+ts**：wave-15 立法「hub stream-ledger ts 权威，沙箱墙钟仅参考」=选定全局参考系；Lamport happens-before=prev-hash 偏序（锚链即因果锥：实例 A 之动作对 B 可见 iff A 之锚已进入 B 读到之链尖）。
- **等时面=链尖快照**：一次读尖=一次等时测量——各实例在同一链尖高度对齐即「等时」；「S-I/2 与 S-I/3 已跑出很远」之物理义=它们的锚已越过 S-I/1 最后读尖之光锥，而**重同步成本=一次读尖**（O(1) 锚龄+差分），非考古。
- **节律不失序**：拍序=链序（T2 滞留纯函数可机检）；凡跨实例协作者，以链尖高度为会合面（rendezvous=相遇，PS1 4/4 实证）。
